# Find/Export SHPATH, this is used by other scripts to find this directory.
export SHPATH=$(dirname $0)

# Run every script in $SHPATH/rc.d in alphanumeric order
for f in $SHPATH/rc.d/*; do
	source $f
done
